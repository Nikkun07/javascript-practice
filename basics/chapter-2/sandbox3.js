//Arrays

// let ninjas = ['shaun', 'ryu', 'chun-li'];

// ninjas[1] = 'ken'; //Overriding String Value inside the array
// console.log(ninjas[1]);

// let ages = [20, 25, 30, 35];
// console.log(ages);

// let random = ['shaun', 'crystal', 30, 20];
// console.log(random);

// console.log(ninjas.length); //Length == How many elements are inside the array

//Array Methods
// let result = ninjas.join('-');
// console.log(result);

// let result = ninjas.indexOf('chun-li');

// let result = ninjas.concat(['ken', 'crystal']); //Concatenation of new values inside the array

// let result = ninjas.push('ken'); //Destructive Method (Changes the original value of a variable)
// result = ninjas.pop(); //Removes a value off the array
// console.log(result);

//NULL & UNDIFINED
// Null = Intentional lack of value
// Undefined = Unintentional lack of value

let age = null;
console.log(age, age + 3, `then age is ${age}`);